DATABASE_URL=postgresql://user:password@localhost:5432/powderseek
ANTHROPIC_API_KEY=sk-ant-...
CORS_ORIGINS=http://localhost:5173

# Required. Used to HMAC-sign session tokens issued by POST /session.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Must be >= 32 characters. Rotating invalidates all existing sessions.
SESSION_SECRET=
